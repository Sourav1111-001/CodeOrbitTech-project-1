Place your chatbot demo screen recording here as `chatbot-demo.mp4`.

To record a demo:
1. Run `python chatbot.py` in your terminal
2. Record the terminal session using any screen recorder (OBS, ShareX, etc.)
3. Save the file as `chatbot-demo.mp4` in this folder
